kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-same-namespace
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: nginx-dep
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: alpine
    ports:
      - port: 80
