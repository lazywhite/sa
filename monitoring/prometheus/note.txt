rules
    recording rules
        预计算, 产生新的series并保存
    alert rules
        expr, 触发报警

检查配置
    promtool check rules /path/to/example.rules.yml

应用新配置
    kill -HUP pid
    http://localhost:9090/-/reload



metric type
    counter
    gauge
    histogram
    summary

datamodel
    metric name and label

job and instance


分布式
    federation

storage
    local-disk
    remote storage
        


pushgateway适用于server无法直接pull node的情况, node post到gateway, 再由server pull

server收集的metrics可通过server:9090/metrics访问
