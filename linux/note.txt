芯片组：南桥，北桥
南桥负责慢速I/O设备和PCI接口，北桥负责CPU,VGA，RAM等速度快的设备
cpu内含控制单元和逻辑运算单元
外频x倍频=频率 频率为1秒内可以进行的工作次数，一次工作可完成数个指令
速度为10进制，容量为2进制
北桥总线称为系统总线，南桥总线称为输入输出总线
总线决定cpu每次工作可以传送的资料量的大小
总线带宽=总线宽度x（Front Side Bus）总线速度(频率)
cpu每次能够处理的数据量称为字组大小（word size），决定了cpu的位数32/64
cpu的设计理念RISC和CISC
双通道即双倍的内存总线宽度
SAN NAS
硬盘的组成
I/O地址与IRQ
SRAM,DRAM,EEPROM
BIOS加载CMOS中记录的设备参数,bios写死的，cmos需要通电保持
操作系统的5大核心功能：
1.system call接口2.进程控制3.内存管理4.文件系统支持5.驱动硬件

BELL,MIT,GE->Mutics-Ken Thompson->Unics(assembling)->Ken&Ritchie->Unix(C)
1.Bill Joy(creator of SUN corperation)->Berkeley Software Distribution
2.AT&T->System V
3.IBM->AIX
在第7版unix发布后，Andrew Tanenbaum写出unix-like的Minix
Richard Mathew Stallman->emacs,gcc,bash,gnu c library
linus凭借bash，Gcc开发出linux

MBR仅有446byte,partition table仅有64byte
软raid
regular file->ASCII;binary;data file
目录的权限的准确意义
开机仅挂载根目录，所以/etc,/bin/,/dev,/lib,/sbin不能挂载到别的分区
mkdir -m 711 ./test
umask touch chattr lsattr
SUID仅可应用在binary program上
SGID可应用在二进制程序或者目录上
SBIT仅可应用在目录上
which type file whereis find
