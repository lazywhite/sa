svnadmin create svn-test
svnserve -d --listen-host 0.0.0.0 --listen-port 2345 -r /root

git svn clone svn://ip:port/svn-test


conf/passwd
[users]
harry = harryssecret
sally = sallyssecret

conf/svnserve.conf
anon-access = none
password-db = passwd



========
svn co --username=<> --password=<> -r 1199 svn://domain:20888/homeland/dev/1.1_glusterfs rpmbuild

